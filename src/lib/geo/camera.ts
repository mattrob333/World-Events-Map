/** At 34° FOV, this shows roughly the middle two-thirds of the sphere. */
export const OPENING_GLOBE_DISTANCE = 2.45;

/** Camera and on-globe jet share this timing so the traveller remains in view. */
export const JOURNEY_TRAVEL_MS = 1800;
export const JOURNEY_ALIGN_MS = 260;
